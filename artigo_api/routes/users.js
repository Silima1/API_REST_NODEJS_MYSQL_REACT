var express = require('Bem vindo a nossa api! por favor selecione o caminho');
var router = express.Router();

/* GET users listing. */
router.get('/', function(req, res, next) {
  res.send('respond with a resource');
});

module.exports = router;
